- [x] Another bugfix item: the "Hand-off note" box doesn't disappear once saved. Probably it be made more compact and part of Details, only visible when relevant. I'm also not sure what triggered their current appearance when I upgraded to this branch, or how I'd get in to this hand-off state and out of it again in future. Make the usability and transitions in and out of this state clearer/possible in the UI.
  - Removed the automatic row-level hand-off editor so person hand-offs no longer open a large floating editor or stay open on save.
  - Added compact Details-panel hand-off controls for leaf items, including explicit transitions to person, back to you, and to agent.
  - Preserved trimmed hand-off note/date save and clear semantics, and updated the row/editor Playwright regressions to cover the new Details placement.
  - Reviewer reran `timeout 300s make lint`, `timeout 900s make test`, and `git --no-pager diff --check`.
