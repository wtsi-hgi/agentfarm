- [x] Moving items still broken. Fix it properly with the skill. Now items can only be moved lower down in their own section instead of higher or to different sections. Add proper regression tests that prove they can be moved anywhere!
  - Kept default Tree projection in persisted manual order across roots, sections, and children while leaving priority ranking to Up Next/Follow Up and preserving explicit sibling-section dependency ordering.
  - Added projection regression coverage for priority ranks trying to reorder Tree, adjusted root-anchor/sticky-details tests to the manual Tree contract, and hardened the Playwright move-anywhere repro with per-drag backend checkpoints.
  - Covered the completed-root edge case and a legacy migration marker compatibility issue exposed by Playwright startup; implementor and reviewer reran `timeout 300s make lint` and `timeout 600s make test`.
