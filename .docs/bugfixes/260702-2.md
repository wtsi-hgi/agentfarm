- [x] it seems like items can only be moved up within their existing section, but not down or to other sections. Items should be able to move anywhere
  - Allowed drops that land on the rendered drag-preview row to persist through the stored preview target and position instead of being discarded as a self-drop.
  - Preserved return-to-original-slot behavior as a no-op and kept the existing drag-origin marker behavior intact.
  - Added rendered regression coverage for downward preview-row drops and cross-section preview-row drops; implementor and reviewer reran `timeout 300s make lint` and `timeout 600s make test`.
