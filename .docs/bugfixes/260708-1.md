- [x] Use the bugfix skill workflow to fix the "Hand-off note" box having its bottom truncated and therefore not usable when it appears for an item near the bottom of a section.
  - Reproduced the clipped hand-off editor with a Playwright section-bottom fixture and captured before/after screenshots.
  - Repositioned the Person hand-off editor as a fixed, viewport-clamped popover aligned to the Ball control so root-section clipping no longer hides the note box or Save button.
  - Added browser hit-test assertions for the Hand-off note textarea bottom and Save button; implementor and reviewer reran targeted e2e, `make lint`, and `make test`.
- [x] Manager view should still provide buttons to view notes and prompts, when any are available
  - Reproduced a manager projection row with available notes and prompt/response entries but no way to open them.
  - Added available-only Notes and Prompt/response buttons to manager projection rows, wired to the existing dialogs.
  - Added Playwright coverage that seeds both content types, switches to Manager view, clicks both buttons, and verifies the dialogs open; implementor and reviewer reran targeted e2e, `make lint`, and `make test`.
