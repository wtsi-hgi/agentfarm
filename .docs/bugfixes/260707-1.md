- [x] Adding a new item to a section, or marking an item done, and possibly other things, has the item in the wrong colour. Use the bugfix skill to fix holistically.
  - Replaced persistent selected/focused row background fills with inset rings so local mutations keep the root product pastel visible.
  - Kept transient drag/drop colour affordances while changing filtered-new-item messaging to a border-only cue.
  - Added a Playwright visual regression that creates, marks done, and state-changes child rows in a coloured root section and verifies the rendered row paint still inherits the section colour.
