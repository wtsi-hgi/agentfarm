- [x] Yes, use the bugfix skill workflow to add AGENTFARM_BACKUP_RETENTION_DAYS. Then follow pr-resolver to address the existing copilot comments and push all changes together and then continue pr-resolver as normal.
  - Added `AGENTFARM_BACKUP_RETENTION_DAYS` with a documented default of 30 days and `0` to keep all backups.
  - Added `run-dev.sh --backup-retention-days` and forwarded the setting to the backend.
  - Pruned only strict timestamped Agent Farm backup files after successful backups, keeping unrelated files and making pruning best-effort.
  - Updated `.env.example`, README, backend config, backup service, launcher, and regression tests.
  - PR comment resolution is tracked by the subsequent pr-resolver workflow, not the bugfix checklist.
