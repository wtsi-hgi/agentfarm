- [x] scratchpad implementation has regressed page load speed; it now takes ~5+s, some loads much longer than others. Improve to <1s. Test with 30+ items in a number of nested sections.
  - Added a combined signed-in home payload so initial page render fetches tree, priority, markers, and scratchpad state in one protected backend request.
  - Removed redundant initial outliner state sync and hoisted static row-control elements so controls remain visible and usable during the measured load.
  - Added an 82-row nested Playwright performance regression that waits for all seeded rows, scratchpad visibility, visible row controls, and full state options before stopping the clock.
  - Verified focused timings under 1s and reviewer reran focused performance Playwright, `timeout 300s make lint`, and `timeout 600s make test`.
- [ ] refreshes are now more likely to trigger the bug of the styles not seeming to apply, so the page renders as a pile of messy stuff along the left side of the page.
  - Reproduction attempt stopped before implementation: could not reproduce a genuine unstyled render in Chromium.
  - Tried 80 reloads, 200 reloads, and 120 cache-disabled reloads of a signed-in 49-row nested outline with scratchpad; computed layout stayed styled with centered shell, desktop grid, row CSS grid, sticky scratchpad, and successful stylesheet requests.
  - Captured final styled screenshots under `.tmp/agent/playwright/refresh-styles-repro-260703-d/` and `.tmp/agent/playwright/refresh-styles-repro-260703-cache-disabled/`.
  - Observed occasional Next dev-server `ECONNRESET` / aborted `POST /` fetch logs during refreshes, but no stylesheet/script failures or browser console/page errors.
- [x] scratchpad contents go as wide as the item list and the column where notes/prompts are displayed, but in notes/prompts view the scratchpad UI is full width. Make it keep its content width, allowing the "New entry"/"New Note" section be full height like Details are.
  - Constrained the docked scratchpad to the notes/prompts history-content column instead of the full viewport while dialogs are open.
  - Kept the New Note/New entry column full height beside the scratchpad by reserving scratchpad space only for the history/content column.
  - Added browser geometry coverage for notes and prompt/response dialogs proving scratchpad width and form-column height.
  - Reviewer reran focused layout Playwright, evidence repro, `timeout 300s make lint`, and `timeout 600s make test`.
- [ ] on page refresh the scratchpad says "Autosaved", which doesn't make sense when no change has been made. When a change is made it says "Saved", but this should fade out after a while so the message isn't permanently there.
