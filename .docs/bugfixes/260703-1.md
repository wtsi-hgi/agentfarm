- [x] Add a note taking box ("scratch pad") at the bottom of the item list that can be minimised to the bottom and resized by dragging up and down. Its contents, size and minimise state are all saved and persisted automatically on any change by the primary user. Changes made by other users are not persisted to db, and other users can't type in the box. Scratchpad needs to be visible when looking at notes and prompts, since its purpose is to collect text from different items/comments together, then past them in to one of these.
  - Added persisted scratchpad storage, API schemas/routes, Zod contracts, and BFF actions with signed-in read access and primary-user-only updates.
  - Added the resizable/minimizable scratchpad UI at the bottom of the item list, with debounced autosave for the owner and read-only manager/viewer behavior.
  - Kept the scratchpad available while notes and prompt/response dialogs are open without covering dialog content, with real-browser geometry coverage.
  - Hardened SQLite connection behavior with WAL, busy timeout, and function-scoped DB dependencies to avoid autosave-related lock failures under e2e load.
  - Added backend, contract/action, rendered UI, and Playwright regression coverage; reviewer reran focused scratchpad Playwright, `timeout 300s make lint`, and `timeout 600s make test`.
