- [x] There is something wrong with your ability to properly test and fix this bug. There has been no change in behaviour and the bug remains. Try again to fix it with the bugfix skill workflow. First step is replicate the bug. If you can't do that, stop and let me know. The test is to have multiple items in a nested section. Create a new one in the middle. Give it a name. Drag the item above the item currently above it. The drag fails (the drag marker moves back down). Refresh shows the item really hasn't changed position. A fix of this bug means the drag doesn't fail, the item is in the position that the drag ghost shows, and remains in that position after a page refresh, and the dependencies of all the items in the section are correct.
  - Repro attempt stopped before implementation: could not reproduce the exact failure on current HEAD.
  - Tried a focused Playwright path with backend-seeded root -> outer section -> inner section -> alpha/beta/gamma, then UI-created and UI-renamed an inserted item in the nested section. The drag ghost showed the intended one-slot-up position, the visible-handle drag persisted after drop, refresh kept the new order, and the automatic dependency chain matched the desired final order.
  - Also tried the first-position variant and a drop-on-preview-row event path; both persisted correctly. A lower-level mouse-drag attempt failed too early because it did not produce the required higher-position ghost, so it was not accepted as a valid reproduction.
  - Reproduced the current failure against a disposable copy of the user's `.tmp/agentfarm.db`: dragging `foo` up produced no usable preview, and dragging it down showed the intended ghost but snapped back without changing API/DB order or automatic dependencies after refresh.
  - Replaced native handle dragging for row moves with a coordinate-driven visible-handle drag path that commits the current preview on mouse release, so Chrome/Safari do not lose the drop when the preview reorders the row under the pointer.
  - Fixed coordinate drop classification so before/after row zones use real row height, preventing drops near `scratch pad` from becoming unintended `inside` moves.
  - Preserved real mouse drag-to-Details dependency creation by marking the Details drop target and routing coordinate mouse releases there through the existing dependency creation path.
  - Added real mouse Playwright coverage for section leaf moves up and down, persisted refresh order, repaired automatic dependency chains, and Details dependency drag/drop. Verified the copied user DB `foo` up/down flows after the fix.
