- [x] Following the recent fixes to item dragging to reorder them, there has been a regression in the ability to drag them to the "Add dependency" box in the details section. Restore this ability and make sure there's a proper test for this so it doesn't regress again.
  - Restored native row dragging into the Details Add dependency drop target by sharing drag-start setup, allowing link/move drops, and exposing the dragged item id synchronously for Details dragover handling.
  - Added a Playwright regression proving a leaf row can be dragged into a populated Details Add dependency target and appears as an explicit dependency.
  - Kept coordinate-driven row reordering intact and stabilized the existing nested-outline performance regression to measure post-response visible readiness while still logging full navigation timings.
