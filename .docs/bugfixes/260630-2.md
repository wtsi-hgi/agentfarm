- [x] address warnings in seen in the logs
- [x] figure out why I can't log in, improving logging if needed
  - Confirmed Make-exported shell-style quoted `.env` values could reach backend settings with quote characters intact, breaking LDAP server/DN values before bind.
  - Normalized one pair of outer shell quotes in settings, preserving `%s` DN templates and meaningful internal quotes.
  - Added safe LDAP auth diagnostics for bind failures, access denial, and missing configuration without logging passwords or bind DNs.
  - Mapped login 401/403 failures to clearer user-safe frontend messages.
- [x] Adding the first root item resulted in Next.js Issues popping up: In HTML, `<div>` cannot be a descendant of `<p>`. This will cause a hydration error. Console Error: `<p>` cannot contain a nested `<div>`.
  - Replaced loading skeleton `div` children inside card title/description text elements with block `span` skeletons.
  - Added a React-rendered regression test that fails on invalid DOM nesting or hydration console warnings from the loading UI.
- [x] "New item" text should not be real text in the text box the user has to delete every time. The text box should be select-and-immediate-type.
  - Focused and selected the first root title field on mount so typing immediately replaces the default title.
  - Added one-shot title focus requests for newly created rows and regression coverage for first-root and Enter-created item selection.
- [x] At some point during adding and deleting root items, "Backend request failed with 404" appeared under "Add comment".
  - Cleared locally deleted subtrees out of selection/render targets so the comment panel cannot keep targeting a deleted item.
  - Ignored stale async comment load/add results after the selected item changes, while preserving errors for the current item.
  - Added a rendered UI regression test for deleting a root while a child comment request returns a stale 404.
- [x] In the default view, everything being equal, hitting return at the end of typing an item name creates a new item above instead of below, and doesn't auto-select the new item so you can just start typing the new item. Item 1 then item 2 created under a root shows item 2 highest in the list, but the movement arrows only offers "move up" for it, which doesn't make any sense. Clicking it doesn't change its position. It is therefore entirely unclear if there is any chained dependency relationship going on, and what the true order of the chain is.
  - Clarification: items in a section that are not sections themselves do have an implicit dependency chain; roots/sections/subsections are not dependency chains by default.
  - Ordering requirement: items in a section always display in dependency-chain order: the order they were typed in. For example, typing "a", pressing return, then typing "b" displays "b" under "a" whenever both are displayed by the current view settings.
  - Manual reordering requirement: manually reordering items in a section alters the implicit dependency chain to match the visual order the user applied.
  - Added live implicit dependency-chain semantics for plain items inside sections, derived from current visual sort order.
  - Preserved independent root/section/subsection siblings unless explicit dependencies exist.
  - Kept priority projection while preserving chained leaf order, and added tests for Enter-created items, move controls, chain metadata/actionability, and reorder-updated chains.
- [x] Items have a "comments" button that doesn't seem to do anything once clicked.
  - Added a one-shot comments-panel focus request when an item's comments button is clicked.
  - The comments panel now scrolls into view, focuses the new-comment input, and shows a focus ring without changing ordinary row-selection focus behavior.
  - Added a rendered UI regression test for comments-button focus and selection behavior.
- [x] Having created an item, there doesn't seem to be any mechanism to change its state.
  - Added an accessible item-state selector in each row using the existing state values.
  - Wired state selection through the existing item patch mutation path.
  - Added a rendered UI regression test that changes a created item's state and verifies the mutation payload and updated control value.
- [x] There is no need to say "Unified Tree" at the top of the page.
  - Removed the redundant top-page eyebrow text while preserving the main page identity and outliner context.
  - Updated the rendered page test to assert the important page context remains without showing "Unified Tree".
- [x] In case 1 manager is given access to multiple employees agent farm web interfaces, or colleagues share their urls, it should be clear who's agent farm this is. Also convert the seperate login page to a utility in the top right of the page to login / logout that shows the currently logged in user and if you're the primary user or a manager role.
  - Added a shared app shell that names the farm owner in the page header.
  - Added a top-right account utility showing unauthenticated sign-in, authenticated username, primary-user/manager role, and sign-out.
  - Exposed public farm-owner context and verified account display from the signed backend session rather than forgeable client claims.
  - Updated login, session, proxy, contract, and auth tests for the shell and logout flow.
- [x] Expand the fact that items can have comments associated with them and displayed on the right to also allow entry of and display of special fields. Root items should have repo URL. All items should have a "description". Added comments should be timestamped. Any state changes should also be shown with timestamps.
  - Added item description and root-only repo URL storage, API contracts, BFF actions, and right-panel editing/display.
  - Added timestamp display for comments and timestamped state-change activity in the details panel.
  - Added a backward-compatible SQLite migration and regression tests for migration, repo URL validation, contracts, comments, state activity, and rendered panel behavior.
- [x] Trying to use the drag handles on the left of items to rearrange them doesn't do anything, just gives next.js issue: In HTML, `<div>` cannot be a descendant of `<p>`. This will cause a hydration error.
  - Made the left grip an accessible draggable row action.
  - Added before/after drop handling that persists through the existing move mutation path.
  - Added rendered drag/drop and invalid DOM nesting regression coverage while preserving button move controls.
- [x] In addition to whatever solution is added to enable setting current state to particular fine-grained values (previous bugfix item), there should be a simple checkbox instead of a dot to the left of all items (including sections and roots) to mark the thing as done. Done items display greyed out and have the least possibly priority.
  - Replaced the old row dot with a checkbox for roots, sections, and items.
  - Checking uses the existing state mutation path to mark `done`; unchecking resets to `not-started` as the conservative base state.
  - Done rows render muted and stay in sync with the fine-grained state selector.
  - Added regression coverage proving done work is deprioritized behind active work while preserving section chain ordering.
- [x] Do improve unchecking a done box to revert to last state.
  - Remembered each item's last active non-complete state before marking it done.
  - Restored that state when the done checkbox is unchecked, falling back to persisted state-change activity after reload and only then to `not-started`.
  - Added rendered UI coverage for checkbox/selector sync, activity refresh, restored priority/styling, persisted-history restore, and empty-history fallback.
- [x] The item Details "Save" button should be disabled until there's a change to save.
  - Derived details dirty state from persisted description and root repository URL values.
  - Disabled and guarded the Details save action while clean, then reset dirty state after save or item selection changes.
  - Added rendered panel regression tests for clean, dirty, post-save, and selection-reset behavior.
- [x] Once saved, a Repository url should be clickable to open the link, perhaps needing an edit button to change it again.
  - Rendered saved root repository URLs as external links with safe new-tab attributes.
  - Added an explicit edit affordance to switch the saved URL back to an input.
  - Preserved empty-root editable state, non-root hiding, and Details dirty-save behavior with rendered tests.
- [x] The Comments button on items just seems to highlight the Details box which for me is always visible. Unless I'm missing something, it still seems pointless. Get rid of it?
  - Removed the redundant row-level Comments action and obsolete details-panel focus wiring.
  - Preserved row selection as the mechanism for driving the always-visible Details panel.
  - Updated rendered tests to cover row selection, adding comments, and stale comment-target protection without the button.
- [x] There needs to be an explicit selectable state for "Feedback" (waiting on user to provide feedback), which is one categery of "blocked waiting on external events", which should gain the "Waiting" indicator on the right of items. There may be other states to add in this same category needing the same Waiting treatment, so build it so it is easy to add new states to that category and have code reuse. There should also be a "Respond" state, meaning we're in feedback loop, but the ball is in my court (the blocker is me, so this is not "Waiting", but probably high priority for me to action next to get the ball rolling.
  - Added `feedback` and `respond` to backend/frontend state contracts and selectors.
  - Added reusable external-waiting metadata so `feedback` shows the Waiting indicator and future waiting states can share the same path.
  - Kept `respond` actionable and priority-boosted without showing Waiting.
  - Added backend/frontend regression coverage for contracts, activity labels, selector options, waiting indicators, priority, and checkbox restore behavior.
- [x] I'm not sure what behavour the "Prompt", "Review" etc. buttons near the top are supposed to have. Ticking Prompt doesn't change anything, Ticking Review hides everything even when I have an item (section in my case) in Review state). The buttons are also in a strange order. Probably more useful is to have an "Up Next" button that only shows actionable (non-waiting) things in priority order. There may be other useful views as well, but I can't think of any right now, so get rid of other buttons in this row.
  - Replaced the confusing state filter row with `Tree` and `Up Next` controls.
  - `Up Next` uses ranked priority items that are actionable, non-complete, not externally blocked, and not in an external-waiting state.
  - Added rendered and projection tests for Feedback hidden, Respond visible/high-priority, Tree return, and review-state visibility in the normal tree.
- [x] Clicking the sign out button gives "Something went wrong / An unexpected error occurred" and Next.js reports `BackendRequestError: Backend request failed with 401` from `backendJson` in `app/page.tsx` after logout.
  - Skipped protected tree/priority fetches when the home page renders signed out.
  - Mapped protected-data 401s after stale/logout sessions back to the signed-out shell without hiding non-auth backend failures.
  - Added page/login regressions for direct signed-out home rendering, logout/stale-session rendering, and login reachability.
- [x] When signed out, there is a sign in button top right as desired, but it doesn't do anything, because there is just a big Sign in box in the middle of the screen. I was imagining nothing in the middle of the screen, and the sign in button pops up a little username password form undeneath. But since the web page displays nothing but the header row when signed out, the central sign in form is fine; just get rid of the sign in button (but keep the "not signed in login required" box top right).
  - Removed the redundant top-right signed-out Sign in control while keeping the `Not signed in` / `Login required` status box.
  - Preserved the central login form and authenticated Sign out control.
  - Updated page/login tests for signed-out and authenticated header behavior.
- [x] The default view (after page refresh) should be the tree fully expanded with all items visible, excluding items done before the most recent marker.
  - Fetched markers for the default page and passed them into the outliner.
  - Made the Tree view start fully expanded while preserving manual collapse/expand and separate Up Next behavior.
  - Hid complete rows with `completed_at` on or before the latest marker, while keeping after-marker, null-completed, and no-marker rows visible.
  - Added projection and rendered regressions for expansion, marker cutoff, visible descendants, and Up Next separation.
- [x] "Viewing farm owner sb10" seems redundant given the main "sb10's Agent Farm" title, plust the info of who is logged in top right. Unless I'm missing something, get rid of it.
  - Removed the redundant farm-owner subtext from the shared app shell.
  - Preserved the owner title and top-right account utility, with rendered shell tests for signed-in, signed-out, and login pages.
- [x] Have a little calendar icon button next to the Marker text box that will auto-fill it with today's date in YYYY-MM-DD format. Change the marker system so same-named markers overwrite each other. Actually, more like delete the old one and add the new one, ie. the same-name marker appears at the end of the list of markers.
  - Added an accessible calendar icon button that fills the marker input with the browser-local `YYYY-MM-DD`.
  - Replaced same-name marker creation by deleting existing rows before inserting the new marker, so the replacement appears latest.
  - Updated live marker UI state and tests for replacement ordering, stale Until clearing, and default tree cutoff.
- [x] In the Details section, put Respository above Description.
  - Moved the root-only Repository block above Description in the Details panel.
  - Added rendered coverage for root field order and non-root Description-without-Repository behavior.
- [x] It shoudn't be so easy to delete things (items, comments, anything else now or in the future). Deletions should give a pop-up confirmation dialogue.
  - Added a reusable accessible destructive confirmation dialog.
  - Gated item row deletes, Ctrl/Delete item deletes, comment deletes, and dependency deletion by removing `>needs:` tokens.
  - Added cancel/confirm coverage proving destructive mutations are not called before confirmation.
- [x] For an item, we need a place to record a series of prompts and responses. These record key interactions with an agent on that task. This is separate to Comments, which are between humans. The prompts and responses will quickly become large and unwieldy and not well suited for display in the Details section with other things. But it will also be the primary thing changing on items most of the time, and in v2 of the app may actually drive and be linked with agents. So perhaps it's an action button on the item row that brings up a large ~full-screen overlay popup dedicated to showing a scrollable timeline history of prompts and responses (clearly identifyable, and easy to add a new one of a desired kind, which picks up timestamp of entry for now). Responses should be able to take the copy/paste of typical agent CLI/App output and make it look good. Ie. interpret headings, lists, tables made of ascii lines etc, actual markdown, and convert to markdown for attractive display.
  - Added backend storage, owner-guarded creation, signed-in reading, API schemas, and BFF actions for item prompt/response timeline entries.
  - Added an item-row action that opens a large scrollable timeline dialog separate from human comments, with prompt/response entry forms and server timestamps.
  - Added safe markdown/terminal-style rendering for headings, lists, code blocks, markdown tables, ASCII tables, and pasted CLI output without injecting raw HTML.
  - Added backend, contract, action, and rendered UI regression coverage; reviewer reran `make lint` and `make test`.
- [x] In addition to the extra Repository detail that root items have, add a Usage section where instructions/ command lines etc. can be put that explain how to test or use the product. Should be easy to copy/paste certain lines in to a terminal. Ideally entry would be markdown with code block support, so this section can display as text instructions plus monospace font terminal commands to copy.
  - Added persisted root-only Usage data through SQLite schema/migration, FastAPI schemas/routes, Zod contracts, and server actions.
  - Included Usage in Details dirty/save/reset behavior while hiding and rejecting Usage updates for non-root items.
  - Rendered saved Usage as safe markdown with fenced code blocks and accessible copy controls for terminal commands.
  - Added backend, contract, action, and rendered UI coverage; reviewer reran `make lint` and `make test`.
- [x] Links in Descriptions and Usage sections should be clickable. Same mechanism as Repository should be used for setting values and then having an edit button to modify, otherwise they present nicely and are internally clickable etc.
  - Changed saved Description and root Usage fields to render in read mode with explicit Edit buttons, matching the Repository pattern.
  - Added safe clickable link rendering for markdown and bare `http`/`https` URLs without raw HTML injection.
  - Preserved Usage markdown/code-block rendering and code copy controls while keeping empty fields easy to enter.
  - Added rendered UI coverage for link display, edit round trips, dirty Save behavior, selection reset, and code-copy preservation; reviewer reran `make lint` and `make test`.
- [x] Clicking on an item with enough activity to make the Details section cause a vertical scroll bar to appear on the page messes up the layout of the filter area, with the x button moving to the next line.
  - Converted the prior Playwright scanner into a deterministic 1089x1080 regression that seeds a details-heavy item, proves the page becomes scrollable, and asserts the clear-filter button stays on the apply-filter row.
  - Kept the existing marker/filter controls while giving marker selects stable capped widths and grouping the change-field, apply, and clear controls so the clear `x` cannot wrap alone.
  - Stacked the marker controls and product finder until the `xl` breakpoint, preventing narrow desktop widths from squeezing both tool groups onto one row.
  - Verified the focused regression failed before the layout fix with the clear button wrapped, then passed after the fix; reran `timeout 300s make lint` and `timeout 600s make test`.
- [x] Signing out doesn't blank the page; it shows an empty item list with ability to create new root node. It should just show the sign in box instead.
  - Gated the home page metrics and outliner on an authorized session, rendering the central sign-in form instead when signed out or when protected data returns 401.
  - Preserved the top-right `Not signed in` / `Login required` account utility without reintroducing a redundant top-right sign-in button.
  - Added Vitest coverage for signed-out, logout, and stale protected-data 401 paths plus Playwright coverage for signing out through the real UI.
  - Reviewer reran `timeout 300s make lint` and `timeout 600s make test`.
- [x] Signing in gives this next.js issue: Console Error

  A tree hydrated but some attributes of the server rendered HTML didn't match the client properties. This won't be patched up. This can happen if a SSR-ed Client Component used:
  - A server/client branch `if (typeof window !== 'undefined')`.
  - Variable input such as `Date.now()` or `Math.random()` which changes each time it's called.
  - Date formatting in a user's locale which doesn't match the server.
  - External changing data without sending a snapshot of it along with the HTML.
  - Invalid HTML tag nesting.

  It can also happen if the client has a browser extension installed which messes with the HTML before React loaded.

  See more info here: [https://nextjs.org/docs/messages/react-hydration-error](https://nextjs.org/docs/messages/react-hydration-error)

  ```
  <Button type="button" size="icon" variant="ghost" disabled={false} aria-label="Copy code ..." ...>
    <button
      data-slot="button"
      className="inline-flex items-center justify-center gap-2 whitespace-nowrap rounded-md ..."
      type="button"
  +   disabled={false}
  -   disabled=""
      aria-label="Copy code block"
      ...
    >
  ```

  Stack includes `components/ui/button.tsx`, `components/markdown-content.tsx`, `components/comments-panel.tsx`, `components/outliner.tsx`, and `app/page.tsx`.
  - Fixed markdown code-copy controls so SSR and the first client render both start with copy disabled, then enable after mount when `navigator.clipboard.writeText` is available.
  - Preserved safe markdown rendering, accessible copy labels, tooltip text, and clipboard copy behavior.
  - Refined the Playwright hydration regression to select its seeded root item, assert the saved Usage code block appears, assert the copy button enables after hydration, and fail on hydration mismatch console errors.
- [x] Instead of clicking on an existing item and hitting return to create a new one and having to click the save icon to create an item without also creating a new one, return should just save changes to the item name. The save icon should be replaced with a "line feed/return" looking icon button that takes over the old return behaviour of creating a sibling item.
  - Changed Enter on existing item titles to save/apply row text without creating a sibling.
  - Replaced the row save affordance with an accessible `Add sibling` return-style icon button.
  - Preserved the old Enter-created-sibling flow on the new button: create below current item, focus it, and select the default title for immediate typing.
  - Updated behavior tests for Enter saving, explicit sibling creation, focus selection, and related delete/dependency confirmation flows; reviewer reran `timeout 300s make lint` and `timeout 600s make test`.
- [x] Instead of having a Save button in the Details section, each field's edit button, when clicked, becomes cancel/save buttons. Make the Repository edit button consistent with the others (displayed on the same line as "Repository", same icon and colour/highlight level).
  - Removed the global Details save button and gave Repository, Description, and Usage their own label-row edit/save/cancel controls.
  - Made Repository use the same same-line edit icon treatment as Description and Usage.
  - Saved only the edited field, disabled field save while clean, and made cancel restore that field's saved draft without touching other fields.
  - Preserved saved Repository links, Description/Usage markdown links, Usage code-copy behavior, root-only field visibility, comments behavior, and selection reset; reviewer reran `timeout 300s make lint` and `timeout 600s make test`.
- [x] "Implement" state is a "Waiting" state, since I don't need to take any action while agent implements the code. Next to "Up Next" add a "Follow Up" button to view items that are waiting on non-me (Feedback and Implement" items).
  - Added `implement` to shared backend/frontend external-waiting metadata, so it is non-actionable, excluded from priority, and shows the existing `Waiting` row indicator.
  - Added a `Follow Up` view alongside `Tree` and `Up Next` that shows non-me waiting work: `feedback`, `implement`, and explicit `blocked_external` items, while keeping `respond` in `Up Next`.
  - Kept waiting-state logic on reusable metadata/item helpers and updated behavior tests for backend priority/tree contracts, projection ordering, rendered controls, and waiting indicators.
  - Verified with `timeout 300s make lint` and `timeout 600s make test`.
- [x] Sign out followed immediately by signing in without refreshing the page doesn't work. You have to sign in a second time.
  - Allowed unauthenticated `POST /` through the Next proxy only when it is a Next Server Action request, so the signed-out central login form can run after logout while ordinary signed-out home posts still redirect to `/login`.
  - Completed the Playwright-only fake LDAP server enough for real `ldap3` direct-bind login by returning a successful LDAP search-done response after bind.
  - Added focused proxy coverage for the narrow server-action allowance and preserved the Playwright re-login regression through the real Next/FastAPI/fake-LDAP path.
  - Verified with focused proxy/Vitest and Playwright checks, then reran `timeout 300s make lint` and `timeout 600s make test`.
- [x] Delete all trace of the mirror feature from the codebase, but leave `.docs` alone. No backwards compatibility, no comments left behind referencing it, and no mention in README.
  - Removed the mirror service, mirror-only tests, config surface, endpoint mutation hooks, README mention, and stale code comments/docstrings.
  - Updated remaining config/dependency tests to cover supported behavior without mirror-only assertions.
  - Left `.docs/v1` and runtime `data/` untouched; no local `data/mirror` directory was present.
  - Verified no feature-specific references remain outside excluded `.docs`/generated paths; reviewer reran `timeout 300s make lint` and `timeout 600s make test`.
