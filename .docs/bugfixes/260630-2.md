- [x] address warnings in seen in the logs
- [x] figure out why I can't log in, improving logging if needed
  - Confirmed Make-exported shell-style quoted `.env` values could reach backend settings with quote characters intact, breaking LDAP server/DN values before bind.
  - Normalized one pair of outer shell quotes in settings, preserving `%s` DN templates and meaningful internal quotes.
  - Added safe LDAP auth diagnostics for bind failures, access denial, and missing configuration without logging passwords or bind DNs.
  - Mapped login 401/403 failures to clearer user-safe frontend messages.
- [x] Adding the first root item resulted in Next.js Issues popping up: In HTML, `<div>` cannot be a descendant of `<p>`. This will cause a hydration error. Console Error: `<p>` cannot contain a nested `<div>`.
  - Replaced loading skeleton `div` children inside card title/description text elements with block `span` skeletons.
  - Added a React-rendered regression test that fails on invalid DOM nesting or hydration console warnings from the loading UI.
- [ ] "New item" text should not be real text in the text box the user has to delete every time. The text box should be select-and-immediate-type.
- [ ] At some point during adding and deleting root items, "Backend request failed with 404" appeared under "Add comment".
