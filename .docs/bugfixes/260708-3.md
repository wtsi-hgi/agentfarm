- [x] Another bugfix item: remove the "Create root" UI in Manager view
  - Hid the bottom root creator while the Manager view is selected, without changing normal Tree/Up Next/Follow Up root creation.
  - Added Playwright coverage proving Manager view exposes no `First root title` or `Create root` controls.
  - Preserved Manager notes/prompts buttons and the owner root-product creator regressions; reviewer reran targeted e2e, `timeout 300s make lint`, and `timeout 900s make test`.
