- [x] The initial "Mouse scrolling while viewing notes or prompts screens shouldn't scroll the whole page, only scrollable components of the visible boxes, or nothing." seems to have broken the desired behaviour more. Now the page scroll wheel doesn't show when viewing notes/prompts: good. But viewing the notes/prompts for an item low in the page (page scroll position low to see the item row) and closing the notes/prompt even without doing any scrolling at all reverts the view to the top of the item list, so we can't see the item near the bottom of the page we were just looking at. Use the bugfix skill workflow to keep the current page position when entering and exiting ~full-page views.
  - Replaced the overflow-based dialog scroll lock with wheel-event containment so page wheel scrolling stays blocked while visible dialog and scratchpad scrollable regions can still scroll internally.
  - Kept the docked scratchpad's in-flow height reserved with an aria-hidden spacer so opening a full-page item dialog does not clamp lower page scroll positions.
  - Extended the Playwright regression to verify lower page position is preserved while opening and after closing notes and prompt/response dialogs.
- [x] Another item to add to your checklist: scroll behaviour in long details sections seems highly inconsistent/random. Sometimes it will scroll the details section as intended, sometimes the details section won't scroll and the whole page (items list) scrolls instead. Lock it to only scroll the details section when the mouse is anywhere over the details section and it has a scroll bar.
  - Added a non-passive wheel listener on the Item details panel so wheel events anywhere over the panel are routed to the panel's internal scroll area when it is scrollable, preventing the item list page from moving.
  - Kept the reproduction Playwright coverage in `frontend/e2e/details-scroll-repro.spec.ts`; it now verifies wheeling over the Details panel chrome advances the internal Details scroll position while `window.scrollY` stays fixed.
  - Verified with `timeout 600s pnpm --dir frontend exec playwright test e2e/details-scroll-repro.spec.ts`, `timeout 600s pnpm --dir frontend exec playwright test e2e/item-dialog-scroll-containment-repro.spec.ts`, `timeout 300s make lint`, and `timeout 900s make test`.
- [x] Full e2e scratchpad prompt docked-width check failed once during `timeout 900s make test`, measuring the prompt dialog as single-column with a full-width scratchpad, then passed in focused scratchpad reruns and the final full gate.
  - Evidence: `e2e/scratchpad-dialog-layout.spec.ts:255` failed once with 1 failed / 47 passed; `timeout 600s pnpm --dir frontend exec playwright test e2e/scratchpad-dialog-layout.spec.ts` then passed 4/4, and the final `timeout 900s make test` passed.
  - Stabilized the layout spec by waiting for the fixed two-column desktop dialog and docked scratchpad geometry before measuring width; verified with the focused repeat, lint, and full test gates.
- [x] Despite having a scroll bar, the details section scroll doesn't actually let you scroll all the way down to see the bottom of the details section when viewing the top of a page with a long item list and a long details section. It should let you scroll all the way down to the bottom of the details section.
  - Added a desktop-only dynamic height cap for the sticky Details panel when the adjacent item list is longer than the viewport, based on the panel's actual visible top edge, so the internal scroll area can expose its bottom from the top of a long page.
  - Added a Playwright regression that seeds a long item list plus a long Details section, wheels over Details from `scrollY = 0`, and verifies the bottom sentinel becomes visible while the page position stays fixed.
  - Reviewed and verified with the focused Details, dialog containment, scratchpad layout, filter layout, lint, and full test gates.
- [x] Running `pnpm --dir frontend test:e2e` without a fresh `PLAYWRIGHT_RUN_ID` can reuse a populated `.tmp/agent/playwright/local/data/agentfarm.db` and fail `app.spec.ts` autofocus; the same command passed after removing only that temp DB.
  - Reproduced the failure with `env -u PLAYWRIGHT_RUN_ID pnpm --dir frontend exec playwright test e2e/app.spec.ts -g "selects the first root default title"` against `.tmp/agent/playwright/local/data/agentfarm.db`; removing only that DB made the same command pass.
  - Changed the Playwright config so direct runs without an explicit `PLAYWRIGHT_RUN_ID` generate a fresh `local-...` scratch/data directory, while explicit run IDs remain deterministic.
  - Added config tests for no-run-id isolation and explicit-run-id preservation; reviewed and verified with focused config/e2e checks, direct no-run-id full e2e, lint, format check, and `make test`.
