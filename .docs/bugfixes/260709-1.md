- [x] The initial "Mouse scrolling while viewing notes or prompts screens shouldn't scroll the whole page, only scrollable components of the visible boxes, or nothing." seems to have broken the desired behaviour more. Now the page scroll wheel doesn't show when viewing notes/prompts: good. But viewing the notes/prompts for an item low in the page (page scroll position low to see the item row) and closing the notes/prompt even without doing any scrolling at all reverts the view to the top of the item list, so we can't see the item near the bottom of the page we were just looking at. Use the bugfix skill workflow to keep the current page position when entering and exiting ~full-page views.
  - Replaced the overflow-based dialog scroll lock with wheel-event containment so page wheel scrolling stays blocked while visible dialog and scratchpad scrollable regions can still scroll internally.
  - Kept the docked scratchpad's in-flow height reserved with an aria-hidden spacer so opening a full-page item dialog does not clamp lower page scroll positions.
  - Extended the Playwright regression to verify lower page position is preserved while opening and after closing notes and prompt/response dialogs.
- [ ] Another item to add to your checklist: scroll behaviour in long details sections seems highly inconsistent/random. Sometimes it will scroll the details section as intended, sometimes the details section won't scroll and the whole page (items list) scrolls instead. Lock it to only scroll the details section when the mouse is anywhere over the details section and it has a scroll bar.
- [ ] Full e2e scratchpad prompt docked-width check failed once during `timeout 900s make test`, measuring the prompt dialog as single-column with a full-width scratchpad, then passed in focused scratchpad reruns and the final full gate.
  - Evidence: `e2e/scratchpad-dialog-layout.spec.ts:255` failed once with 1 failed / 47 passed; `timeout 600s pnpm --dir frontend exec playwright test e2e/scratchpad-dialog-layout.spec.ts` then passed 4/4, and the final `timeout 900s make test` passed.
- [ ] Despite having a scroll bar, the details section scroll doesn't actually let you scroll all the way down to see the bottom of the details section when viewing the top of a page with a long item list and a long details section. It should let you scroll all the way down to the bottom of the details section.
