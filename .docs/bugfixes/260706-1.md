- [x] Use the bugfix skill workflow to add this feature: automated on-line backups of the database at most every  10 mins (configurable), only if there have been db writes since the last backup. Backup location configurable; not set means no backup. Configuration via CLI option and env var. Update .env.example to describe all env vars used.
  - Added configurable SQLite online backups with `AGENTFARM_BACKUP_DIR`, `AGENTFARM_BACKUP_INTERVAL_SECONDS`, and matching `run-dev.sh` CLI options.
  - Wired committed writes to mark the database dirty, with a FastAPI lifespan scheduler that backs up at most once per configured interval and only when writes occurred since the last successful backup.
  - Kept backup attempts best-effort so invalid backup targets do not break already committed writes.
  - Updated `.env.example` and README environment documentation, plus backend and launcher regression coverage.
