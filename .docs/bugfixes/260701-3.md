- [x] Don't show the dependencies on the item row, only in the details section
  - Removed row-level dependency slug text while keeping readiness indicators and row controls intact.
  - Updated the dependency Details Playwright repro so dependencies are asserted in Details, not item rows.
  - Verified with `timeout 300s make lint` and `timeout 600s make test`.
