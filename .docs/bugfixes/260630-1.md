- [x] Add the ability to set in .env that the frontend should listen on 0.0.0.0.
  - Added `FRONTEND_HOST` support through the repo-root `.env`, `make run`, and `run-dev.sh`.
  - Added launcher regression coverage proving `.env` values are forwarded and `0.0.0.0` is treated as a bind address, not a browser URL.
- [x] How are you expecting me to login to the website with my ldap password on a http website? That's obviously a big security bug.
  - Started the Next dev frontend over HTTPS with the repo-local TLS certificate/key used by the backend.
  - Updated Playwright, shell tests, examples, and README guidance so LDAP login is reached through HTTPS URLs.
