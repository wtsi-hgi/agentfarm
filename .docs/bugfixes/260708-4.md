- [x] Put "All products" filter dropdown in alphabetical order.
  - Sorted root product filter options alphabetically by title with a stable id tie-breaker, without changing tree/projection row ordering.
  - Added Playwright coverage that seeds products in non-alphabetical stored order and verifies the rendered filter dropdown order.
- [x] Rather than show errors when viewer role tries to do write operations, show them an altered read-only view that lacks all write operations. Eg. don't show the item drag affordance, the "Add sibling" button, the state as a dropdown menu, the "delete" button etc.
  - Passed owner editability from the signed session into the outliner and hid viewer write affordances across rows, root creation, marker creation, Details, comments, dependencies, notes, and prompt/response dialogs.
  - Preserved viewer read-only navigation and viewing controls including view modes, product filter, item search, marker filtering, Details display, existing notes/prompts, and scratchpad.
  - Added Playwright and rendered regression coverage for the viewer read-only surface and owner marker-control behavior.
- [ ] Section items should have their "Add sibling" button replaced with a "Add child" button to create an item in their section. Add such a button to root nodes as well.
- [ ] Mouse scrolling while viewing notes or prompts screens shouldn't scroll the whole page, only scrollable components of the visible boxes, or nothing.
