- [x] all changes (new items, saving changes to items) on the web UI becoming unusably slow the more items there are. It's only of barely acceptable performance with <5 items. At 20+ items it is debilitating. Make speed much faster even at 1 item, and make speed largely independent of number of items. If db schema has to change, my next `make run` after pulling your fix on my other machine should upgrade my existing db and make it fast without losing any of my data.
  - Replaced per-item tree/priority walks with a set-at-once backend projection for completeness, actionability, downstream leverage, and priority ranking.
  - Added idempotent SQLite indexes for item parent-order and dependency lookups; startup migrations create them on existing databases without changing data.
  - Made create-item and patch-item server actions skip synchronous home-route revalidation, with the outliner merging returned item rows locally so new/saved rows update immediately.
  - Converted the timing reproduction into stable backend query-count regression coverage plus a many-row e2e edit smoke.
- [ ] Adding a sibling item to a root item should make the sibling appear beneath the root item, not above it.
