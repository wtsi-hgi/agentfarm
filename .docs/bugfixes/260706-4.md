- [x] Improve clarity by separating out the root sections in to distinct disconnected blocks on different colour backgrounds (auto applied subtle pastels designed to be as different from each other as possible, but stable based on the name of the root item).
  - Grouped each visible depth-0 product and its descendants into a separated root block with subtle pastel backgrounds and inter-section gaps.
  - Made the background tone a stable hash of the product name, independent of neighboring products or visible order.
  - Added rendered stability coverage and a Playwright visual repro verifying distinct painted root backgrounds and disconnected blocks.
- [x] In "Tree" view show products in alphabetical order. "Up Next" and "Follow Up" can show them in priority order. It becomes meaningless to drag root items for the purposes of reordering them since they are never shown in dragged order, so disable that ability, while retaining the ability to drag them to "Add dependency" boxes.
  - Sorted Tree-view root products alphabetically by title with an id tie-breaker, including roots with children and explicit root dependencies.
  - Disabled root structural row reorder previews, drops, and keyboard reorder shortcuts in Tree while preserving non-root dragging/reordering.
  - Added Playwright and rendered regressions proving alphabetical root order, root reorder no-op behavior, and root drag-to-Details dependency creation.
- [x] Hide the "Done" checkbox on root items.
  - Hid the accessible `Mark item done` checkbox on root rows while retaining the fixed grid slot for alignment.
  - Preserved non-root checkbox behavior, done-state mutations, restore behavior, and dark-mode checked styling.
  - Added rendered and Playwright regressions covering root absence and child/non-root checkbox preservation.
- [x] Instead of the "Add Sibling" button on root items, have the same bit of UI for when there are no items at the bottom of the tree, to allow adding new products.
  - Removed root-row `Add sibling` controls while preserving non-root sibling creation.
  - Reused the existing first-root creator at the bottom of the tree to add new root products, without duplicating the empty-tree creator or stealing focus when products exist.
  - Updated rendered and Playwright coverage for root product creation, non-root sibling creation, root ordering, and performance fixture readiness.
- [x] The dropdown menu that allows selecting a product currently only highlights the product item and does a poor job of moving the view to that item. Instead, turn it in to a filter so that only the chosen product is displayed (in any view mode). There should be a way to easily disable the filter to show all products again.
  - Converted the product selector into a persistent `Filter by product` control with an accessible `Show all products` clear action.
  - Applied the selected product root subtree across Tree, Up Next, and Follow Up while preserving priority filtering inside the product.
  - Kept item search as a jump action, clearing conflicting product filters when needed, and added coverage for invalid/deleted product filter cleanup.
- [x] Copilot PR #7 review: remove confusing duplicate `onDragStart`/`onDragEnd` plumbing from `OutlinerRow` now native dragging is handled by the row wrapper and the drag handle is `draggable={false}`. Source: https://github.com/wtsi-hgi/agentfarm/pull/7#discussion_r3530160148
  - Removed dead optional drag-start/end props from `OutlinerRow` and stopped passing them from the outliner.
  - Left wrapper-level native drag and coordinate drag behavior intact.
  - Added no new cleanup-specific test because supported behavior did not change; existing drag, reorder, dependency, lint, and full test gates passed.
- [ ] Copilot PR #7 review: replace the fixed `waitForTimeout(250)` after dependency `dragTo` with waiting on the specific dependency DOM change before taking the screenshot. Source: https://github.com/wtsi-hgi/agentfarm/pull/7#discussion_r3530160174
