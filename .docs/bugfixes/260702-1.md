- [x] Similar to the button and ~full screen UI for entering prompts/responses, have a new system for recording notes. Still entered and presented as a series of dated messages that support and render markdown. Each note is editible after initial creation.
  - Added persisted item notes with owner-only create/edit, signed-in read access, API schemas/routes, Zod contracts, and BFF server actions.
  - Added a row notes action and large scrollable notes dialog that lists dated markdown notes, creates new notes, and edits existing notes.
  - Added backend, contract/action, and rendered UI coverage; implementor and reviewer reran `timeout 300s make lint` and `timeout 600s make test`.
- [x] Prompts and notes screens should include section breadcrumbs before their item titles
  - Added a shared dialog heading that renders ancestor breadcrumbs before the selected item title.
  - Passed parent-chain breadcrumbs into both prompt/response and notes dialogs from the outliner.
  - Added rendered coverage for nested prompt and notes dialogs; implementor and reviewer reran `timeout 300s make lint` and `timeout 600s make test`.
- [x] On the main view, indicate when an item has available notes or prompts by changing look of icon.
  - Added tree-row availability flags for notes and prompt/response entries through backend schemas and frontend contracts.
  - Updated row note/prompt buttons with active visual and accessible states, including immediate no-refresh updates after new entries are created.
  - Added backend, contract, rendered UI, and Playwright light/dark visual coverage; implementors and reviewer reran `timeout 300s make lint` and `timeout 600s make test`.
- [x] Get rid of the indent/outdent buttons on item rows, replacing this ability with drag and hold over item to indent underneath it (the successful indent showing in the ghost), outdent achieved by draggin item next to an item of desired indentation level. Generally when dragging to rearrange, the ghost should clearly show indentation level of the new position.
  - Removed visible row indent/outdent buttons while preserving keyboard drag-handle reordering.
  - Added before/inside/after drag zones so hover previews can indent under a parent or outdent next to a desired-level sibling, with drops persisted through the existing move path.
  - Added rendered and Playwright geometry coverage for removed buttons, indent/outdent drops, and real-browser ghost indentation; implementors and reviewer reran `timeout 300s make lint` and `timeout 600s make test`.
- [ ] Ticked done checkboxes should look greyed out. Not bright white as they are in dark mode.
- [ ] When pasting in the following to a Response box, it renders as "1. [...]" for each item. Add handling to convert this "bad" markdown input to "good", so it renders as "1. [...]" then "2. [...]" etc.
