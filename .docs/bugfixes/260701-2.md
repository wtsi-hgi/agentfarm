- [x] Sections shouldn't have state menus; don't show them or let their state affect anything. Whatever state they have should be remembered in case the section becomes a normal item again. They should still have a done checkbox that does affect things as normal.
  - Hid the item-state selector for rows that currently have children while leaving the done checkbox visible and active.
  - Made frontend readiness styling and Follow Up filtering ignore a section's stored non-complete state, while still treating `done` via the existing checkbox/state mutation path.
  - Kept the stored state unchanged while an item is a section, so the selector shows the old leaf state again if the item later has no children.
  - Added rendered UI coverage and adapted the Playwright repro; verified with `timeout 300s make lint` and `timeout 600s make test`.
- [x] There should be some kind of UI for seeing, setting and removing dependencies between items (excluding the implicit dependency chains of items in the same section). I'm not sure what this could look or behave like. If you know of a good system, try it. Otherwise, try out a dependencies section in the Details view that you can drag items in to (using the existing drag things used for reordering items) to add a dependency, and that you can hit the edit icon for that subsection to drag them out to remove. When a sibling section is dependent on another it is shown as Waiting and displayed after the dependency.
  - Added a Details Dependencies section that lists only explicit edges by target title, slug, and outline context.
  - Added direct edit/remove controls plus a drag/drop Details target and an accessible select/add fallback for creating explicit dependencies.
  - Kept `>needs:` row text behavior intact while updating local dependency labels, readiness, and dependency-aware sibling-section display order without creating automatic explicit chains.
  - Added Vitest and Playwright behavior coverage; verified with `timeout 300s make lint` and `timeout 600s make test`.
- [x] The "Up Next" and "Follow Up" views should still show the sections the shown items belong to; the tree hierachy should still be there (but only showing parts of the tree containing shown items).
  - Kept the filtered views scoped to matching Up Next / Follow Up work while adding ancestor rows for the tree path to those matches.
  - Preserved nested indentation and collapse behavior for filtered context rows, and pruned unrelated siblings and branches that do not contain shown items.
  - Added unit coverage plus the Playwright repro for root -> section -> ready/waiting child paths; verified with `timeout 300s make lint` and `timeout 600s make test`.
- [ ] Instead of implicit dependency chain of siblings in a section, it should now be something that is explicitly added and removed automatically when items are created in a section or moved within a section or to new sections. Using the new (see previous bugfix item) dependency UI, this automatic dependency can be removed like any other. Adding a dependency of an item in the same section that is currently below it will auto-move the item to beneath that one, without changing the dependency of the item that previously immediately followed it. Manually moving the item somewhere else will once again automatically remove and recreate appropriate dependency. When upgrading to this version, `make run` should auto-create as a one off the explicit dependencies between sibling items. To be clear, sections do not get any automatic dependency creation or removal when they are moved.
  - Not completed after five fix/review cycles. The promising implementation added visible automatic chain edges, startup backfill, section-to-leaf repair, and cycle guards, but final review found an unsafe migration ownership edge.
  - Blocker: without a reliable ownership marker for pre-existing dependency rows, the migration could misclassify user-created predecessor-looking explicit dependencies as automatic and later delete them during chain repair.
  - Next viable approach: introduce automatic-chain ownership before/backfilling rows in a way that preserves all pre-existing user explicit rows as user-owned, then rerun the automatic-chain, lower-target move, section-to-leaf, migration-idempotence, and cycle-regression suite.
- [x] when the list of items is long enough that there is a vertical scroll bar and you scroll down to see lower items, the details section should remain visible instead of scrolling off the top of the page.
  - Kept the Playwright repro as behavior coverage and tightened it to assert the Details panel remains within the viewport while lower rows are visible.
  - Made the desktop Details panel sticky, self-starting, and viewport-capped so the long outliner can scroll without stretching the panel off-screen; mobile keeps the stacked in-flow layout.
  - Adapted the marker/filter layout coverage for tall Details content by verifying the panel scrolls internally after it sticks while the filter controls stay on one row.
  - Verified with focused Playwright repros, `timeout 300s make lint`, and `timeout 600s make test`.
- [ ] save UI space by getting rid of the up and down arrows on item rows. improve item dragging to new locations by showing a ghost of what it will look like (exactly where the item will move to and how it shifts other items aside) if you were to let go of the drag.
